----  LAYOUTS  ----
hl.config({
  master = {
    new_status = "inherit",
    mfact = 0.5,
    orientation = "left",
  },
})

hl.config({
  dwindle = {
    preserve_split = true, -- You probably want this
  },
})

hl.config({
  scrolling = {
    fullscreen_on_one_column = true,
  },
})

